建议采用“为什么演进 → 现在是什么 → 如何运行 → 能做什么 → 不能做什么 → 如何使用”的叙述逻辑。

这比直接按照Python模块、C++模块逐一介绍更自然，因为听众首先需要理解：为什么会出现两个版本，以及两个版本共同构成了怎样的仿真体系。

# 一、推荐的总体叙事主线

```
业务背景
  ↓
为什么需要仿真器
  ↓
Python版本解决了什么问题
  ↓
Python版本遇到了什么限制
  ↓
为什么发展出C++版本
  ↓
当前双版本的整体定位
  ↓
仿真器内部如何执行一条NPU程序
  ↓
主要架构模块如何协作
  ↓
仿真器能够验证什么
  ↓
仿真器不能代表什么
  ↓
不同场景应该如何选择和使用
```

可以浓缩成三个篇章：

## 第一篇：从Python原型到C++工程化

回答：

> 为什么会有这个仿真器？为什么后来又建设C++版本？

## 第二篇：当前仿真器如何工作

回答：

> 程序进入仿真器以后，内部发生了什么？

## 第三篇：结果可以说明什么

回答：

> 仿真器能解决哪些问题，又有哪些结论不能由仿真器给出？

------

# 二、第一篇：先讲演进背景

## 1. 从NPU研发问题开始，而不是从语言开始

开篇不要直接说“我们先写了Python，后来改成C++”。

建议先讲实际需求：

- NPU编译器生成的是专用指令流；
- 在RTL、FPGA或芯片上定位问题成本较高；
- 需要一个可观察、可重复执行的软件模型；
- 需要验证指令功能、数据结果、存储访问和同步关系；
- 需要在硬件资源有限时支持编译器快速迭代。

第一层结论：

> 仿真器的首要价值，是在编译器与硬件之间建立一个低成本、可观察的软件验证环境。

此时再引出Python版本，会显得顺理成章。

------

## 2. Python版本为什么先出现

这一部分应该肯定Python版本的历史价值，避免把它讲成“失败的旧版本”。

建议表述：

> 项目初期最重要的目标是快速建立ISA功能模型，及时支持编译器调试。因此首先选择Python实现。

Python适合早期阶段的原因：

- 开发速度快；
- 修改和调试方便；
- NumPy适合实现张量功能；
- 容易打印寄存器和内存状态；
- 容易增加Trace和诊断逻辑；
- 便于快速跟进ISA变化；
- 适合作为功能参考模型。

Python版本主要解决了：

- 指令解码和功能执行；
- CT、LD、LW、ST、DT、VP、PE、SI、SO等executor；
- 寄存器和存储模型；
- 多执行单元mail同步；
- 单核、多核及多Die功能验证；
- Dump和Golden Compare；
- Trace、race和timing stress诊断。

第二层结论：

> Python版本完成了“从无到有”，建立了仿真器的ISA语义基础和调试能力。

------

## 3. Python版本后续遇到的限制

这里要将问题分成不同类别，不能简单归结为“Python慢”。

### 性能和规模限制

- 解释器及对象管理开销；
- 大量动态指令的调度成本；
- JSON Trace产生较大时间和内存开销；
- 大批量回归和重复Launch效率受限；
- 编译器做大量schedule搜索时调用成本较高。

### 并发确定性限制

- 多线程行为受GIL影响；
- NumPy/BLAS可能创建内部线程；
- 宿主机操作系统调度影响执行交错；
- `sleep + wall clock`容易产生运行抖动；
- 低概率并发问题不容易稳定复现。

### Runtime集成限制

- 与C/C++ Runtime的数据结构和生命周期存在转换成本；
- resident memory、共享内存和Daemon集成不够自然；
- 跨Device Port Fabric需要更稳定的ABI；
- 长期运行服务需要更强的资源管理能力。

### 性能建模限制

- 宿主机实际时间与NPU模拟时间容易混淆；
- 不容易稳定表达逻辑周期；
- 指令重叠、Issue Gap和多条指令in-flight需要更明确的调度模型。

这里的关键表述应是：

> Python版本的限制，不代表其功能模型失效，而是项目目标从“快速功能验证”扩展到了“高吞吐、确定性、Runtime集成和性能建模”。

------

## 4. 为什么发展出C++版本

在讲完限制后再介绍C++，逻辑上就是“需求升级推动架构升级”。

C++版本的建设目标：

- 降低长指令流和批量回归的调度开销；
- 建立确定性的离散Tick/Event模型；
- 与Runtime共享resident memory；
- 支持Daemon常驻和异步Launch；
- 支持Port Fabric和跨Device地址路由；
- 支持二进制Trace和结构化分析；
- 为可校准性能模型提供基础；
- 保持对Python参考语义的对齐。

重要的是避免说：

> Python做不到，所以用C++重写。

更准确的表述是：

> 随着应用场景从单Case功能调试扩展到Runtime集成、批量执行和性能分析，仿真器需要新的执行架构，因此发展出C++实现。

------

## 5. 当前不是简单的新旧版本关系

第一篇最后应给出当前双版本定位，这是后续架构介绍的前提。

| 维度     | Python版本              | C++版本                     |
| -------- | ----------------------- | --------------------------- |
| 主要定位 | 功能参考、日常调试      | 工程化执行、Runtime集成     |
| 优势     | 灵活、可观察、修改快    | 确定性、吞吐、常驻服务      |
| 调度模型 | 串行或多线程            | 离散Tick/Event              |
| 运行入口 | Case、Python Daemon     | Resident Daemon、One-shot   |
| Trace    | 诊断丰富                | 二进制、高吞吐、可分级      |
| 并发诊断 | 在线Race、Timing Stress | 确定性调度、依赖图          |
| 性能分析 | 有限、受宿主机影响      | 可配置、可校准逻辑时间      |
| 推荐用途 | 单Case问题定位          | 批量回归、Runtime、性能模型 |

这一部分的总结句：

> 当前架构不是“C++完全取代Python”，而是Python承担参考模型和深度调试，C++承担确定性执行、系统集成和性能扩展。

如果未来组织已经决定完全以C++为生产主线，也可以改成：

> Python保留为Reference Model，C++作为生产仿真器主线。

------

# 三、第二篇：再讲当前架构如何工作

讲完演进历史后，不建议立刻分别展示Python类图和C++类图。应该先讲一条与语言无关的统一仿真流程。

## 6. 先建立统一功能模型

建议先展示：

```
编译器输出
  指令流 + 初始化数据 + Runtime参数
                │
                ▼
             仿真入口
                │
                ▼
       系统、Core与存储初始化
                │
                ▼
          取指、译码和分派
                │
                ▼
       Executor执行和同步控制
                │
                ▼
       L2、DDR与远端存储访问
                │
                ▼
        Trace、Dump与结果比较
```

这页解决听众的基础问题：

> 不管Python还是C++，一个Case进入仿真器后，基本生命周期是什么？

之后再解释两种实现如何完成这条流程。

------

## 7. 按职责分层介绍总体架构

建议将当前仿真器抽象为六层：

```
┌────────────────────────────────────┐
│ 1. 接入层：Case / Runtime / Daemon │
├────────────────────────────────────┤
│ 2. 系统层：Device / Die / Core     │
├────────────────────────────────────┤
│ 3. 控制层：Fetch / Decode / CT     │
├────────────────────────────────────┤
│ 4. 执行层：各类Executor            │
├────────────────────────────────────┤
│ 5. 存储互连层：L2 / DDR / Fabric  │
├────────────────────────────────────┤
│ 6. 诊断层：Trace / Analysis / Dump│
└────────────────────────────────────┘
```

建议先讲层级职责，再讲Python和C++的实现差异。

这样不会让听众陷入具体目录和类名。

------

## 8. 接着讲C++版本的端到端执行流程

由于当前汇报重点很可能是新架构，C++执行流程可以作为主体：

```
Runtime / One-shot
       │
       ▼
Resident Daemon
       │
       ▼
Per-Device Worker
       │
       ▼
SystemSimulator
       │
       ▼
Core Tick Scheduler
       │
       ▼
CT Dispatch
       │
       ▼
Executor Pipeline
       │
       ▼
Memory Router / Port Fabric
       │
       ▼
Trace / Conflict / Dump / Compare
```

建议按以下顺序解释：

1. Runtime或One-shot准备内存；
2. 向Daemon注册Device；
3. 提交异步Launch；
4. Device Worker创建执行上下文；
5. SystemSimulator组织Core、Memory和Fabric；
6. Scheduler推进逻辑Tick；
7. CT负责取指和分派；
8. Executor负责计算、搬运和同步；
9. Memory Router完成本地和远端访问；
10. 执行结束后生成结果和诊断报告。

------

## 9. 然后选择四个核心机制深入解释

不建议平均介绍所有模块，应抓住最能体现架构价值的四个机制。

### 机制一：取指、译码与Executor分派

解释：

- CT如何取得下一条指令；
- Decoder如何得到指令字段；
- 如何判断目标executor；
- 如何检查mail、busy和Issue Gap；
- 条件不满足时如何stall；
- 发射后何时更新PC。

### 机制二：确定性Tick/Event调度

解释：

- 逻辑Tick与宿主机时间无关；
- 每个executor维护执行状态；
- 指令具有Latency和Issue Gap；
- 可以模拟并行重叠；
- 无事件区间可以fast-forward；
- 相同输入得到稳定执行顺序。

这一机制解释为什么C++版能够建立性能仿真框架。

### 机制三：存储与Port Fabric

解释：

- Core-local L2；
- GDG和Weight DDR；
- Memory Router；
- Port Window；
- Topology决定端口目标；
- 远端访问最终归一化到目标Device和Core。

### 机制四：同步与Effect

解释：

- mailbox send/receive；
- CT_SEND/WAIT；
- SISO；
- 同executor FIFO；
- 指令完成时提交memory effect；
- dependency graph根据这些关系判断RAW、WAR和WAW。

------

## 10. 最后讲输出和运行模式

介绍架构之后再讲四种模式，听众才会理解为什么有这些模式：

- Functional：执行真实数据，验证结果；
- Trace-only：执行控制和时序，跳过大部分Payload；
- Analysis-only：规划Memory Effect并检查依赖；
- Full-debug：同时执行Payload、Trace和冲突分析。

这一页实际上是架构实现和功能边界之间的过渡页。

------

# 四、第三篇：最后讲能做什么、不能做什么

功能边界不要只放一页“限制列表”。建议从仿真结论的可信度展开。

## 11. 先讲能做什么

按照使用场景分类，不按模块分类。

### 编译器输出验证

- 指令能否正确译码和执行；
- 寄存器、地址和Shape参数是否合理；
- 数据搬运和计算结果是否符合Golden；
- 动态循环和分支是否正确；
- mail和同步是否满足。

### 多Core和存储验证

- 多executor并行关系；
- Core之间的同步；
- L2、GDG和Weight DDR访问；
- 本地和远端地址路由；
- Port Fabric连接关系；
- 动态执行中的内存冲突。

### 调试和诊断

- 指令Trace；
- Executor Trace；
- Memory Dump；
- Golden Compare；
- Race和Timing Stress；
- Dependency Conflict Analysis；
- 失败指令和地址定位。

### Runtime联调

- Device注册；
- Resident Shared Memory；
- 异步Launch；
- Entry Address；
- Launch状态；
- Snapshot和结果读取。

### 抽象性能分析

- 指令Latency；
- Issue Gap；
- Executor占用；
- 流水重叠；
- Mail和Barrier等待；
- 关键路径；
- 基于工作量的Cost估算。

------

## 12. 再讲不能做什么

建议按“不能替代什么”组织，比简单说功能缺失更容易被接受。

### 不能替代RTL仿真

不能提供：

- 信号级波形；
- 逐流水寄存器状态；
- X/Z传播；
- 复位、CDC和时钟门控；
- 全部RTL微架构细节。

### 不能替代性能签核

未经硬件校准时，不能直接保证：

- 总周期与芯片一致；
- DDR和NoC行为一致；
- Executor实际吞吐一致；
- 最终TOPS或端到端性能一致。

### 不能替代静态或形式化证明

动态执行只能覆盖：

- 当前输入；
- 当前分支；
- 当前循环次数；
- 当前地址和Shape。

不能证明所有输入和路径都安全。

### 不能自动定位所有根因

Golden失败说明结果不一致，但不能自动断言：

- 一定是编译器错误；
- 一定是Runtime错误；
- 一定是仿真器错误；
- 错误来自哪个编译Pass。

------

## 13. 最后讲结果如何解读

建议用一张“结果—结论—不可外推”的表收束边界。

| 结果            | 可以说明                   | 不能说明           |
| --------------- | -------------------------- | ------------------ |
| Golden通过      | 当前输入下结果符合预期     | RTL和芯片一定正确  |
| 无冲突报告      | 已记录依赖能够证明访问有序 | 所有并发交错都安全 |
| 出现RAW/WAR/WAW | 模型不能证明访问有序       | 硬件一定算错       |
| Trace周期更短   | 当前Cost模型下关键路径更短 | 芯片一定更快       |
| Python/C++一致  | 两个软件模型对该Case一致   | 两边一定都符合RTL  |
| 多次回归通过    | 样本范围内未复现问题       | 不存在低概率问题   |

这页是整个功能边界说明的核心。

------

# 五、推荐的最终章节顺序

建议按以下顺序编排PPT：

## 第一章：背景与演进

1. 仿真器在NPU工具链中的位置  
2. 为什么需要功能仿真器  
3. Python版本的建设背景和主要贡献  
4. Python版本遇到的限制  
5. C++版本的建设目标  
6. 当前Python/C++双版本定位

## 第二章：架构与执行

1. 仿真器统一功能模型  
2. 当前系统总体分层架构  
3. C++版本总体架构  
4. 一次Launch的端到端流程  
5. 取指、译码与Executor分派  
6. Tick/Event调度机制  
7. 存储系统与Port Fabric  
8. Mailbox、多Core同步与Effect  
9. Trace、冲突分析和结果输出  
10. 四种执行模式

## 第三章：能力与边界

1. 仿真器能做什么  
2. 仿真器不能做什么  
3. 性能仿真的能力与边界  
4. 仿真结果的正确解读  
5. 不同场景的推荐使用方式  
6. 总结与后续演进

------

# 六、整场汇报的核心观点

建议整份PPT围绕以下三个观点展开。

## 观点一

> Python版本建立了ISA功能参考模型，C++版本是面向确定性执行、Runtime集成和性能扩展的工程化演进。

这样既解释了历史，也避免把Python简单描述为需要淘汰的版本。

## 观点二

> 当前仿真器的核心不是某一种语言，而是取指译码、Executor执行、存储路由、同步依赖和诊断分析共同构成的NPU抽象执行模型。

这样架构介绍不会变成语言对比。

## 观点三

> 仿真器可以验证抽象硬件语义和当前动态执行结果，但不能自然外推为RTL等价、全路径正确或芯片性能准确。

这是功能边界说明的最终落点。

------

# 七、建议本轮先确认的三个问题

在继续编写具体PPT内容前，建议先确定：

1. 汇报重点更偏“当前C++架构”，还是“Python到C++的完整演进”？
2. 汇报对象是编译器和仿真器研发人员，还是管理者及跨团队人员？
3. 是否希望把C++定位成最终主版本，Python仅作为Reference Model？

我的初步建议是：

- 演进背景占20%；
- 当前架构和执行流程占45%；
- 功能能力与边界占30%；
- 后续方向占5%。

这样既能解释为什么存在两个版本，又不会让历史背景抢占架构说明的主体。