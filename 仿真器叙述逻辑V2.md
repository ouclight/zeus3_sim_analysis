基于这三个确认，PPT叙述逻辑应调整为：

> 以“仿真器能力体系”为主线，Python和C++作为两种不同实现方案平行介绍；不讲成版本替代，也不做技术路线站队。

面对管理者和跨团队人员，应减少类名、函数名和代码细节，重点回答：

- 为什么需要仿真器；
- 仿真器在研发流程中解决什么问题；
- 当前两种实现分别如何组织；
- 两者为什么采用不同架构；
- 两者各自适合什么场景；
- 仿真结果可以支持哪些判断；
- 哪些判断不能由仿真器直接给出。

# 一、建议采用的核心叙事

```
为什么需要仿真器
        ↓
仿真器在NPU研发流程中的位置
        ↓
当前形成了Python和C++两种实现
        ↓
两种实现共同遵循什么功能模型
        ↓
Python架构如何实现
        ↓
C++架构如何实现
        ↓
两种实现有哪些共同点与差异
        ↓
当前仿真器整体能做什么
        ↓
不能做什么
        ↓
不同场景如何选择
```

整个叙述过程中，Python和C++的关系建议定义为：

> 两种实现服务于同一套NPU指令语义，但在执行模型、工程形态、诊断能力和系统集成方式上有所不同。

避免使用：

- “旧版”和“新版”；
- “Python被C++替代”；
- “Python不行所以重写”；
- “C++是最终版本”；
- “C++一定更准确”。

建议使用：

- “Python实现”和“C++实现”；
- “两条实现路径”；
- “不同工程侧重点”；
- “能力互补”；
- “适用场景不同”。

------

# 二、推荐的三篇式结构

## 第一篇：为什么需要仿真器

重点回答“价值和定位”。

## 第二篇：当前仿真器是如何实现的

先讲统一模型，再平行介绍Python和C++架构。

## 第三篇：仿真器能做什么、不能做什么

从使用者角度说明能力、边界和适用场景。

这种结构适合管理者和跨团队人员，因为它先给价值，再给架构，最后给使用结论。

------

# 三、第一篇：背景与定位

## 第1页：封面

标题建议：

> ZEUS-V3 NPU仿真器
> 架构实现与功能边界

副标题：

> Python与C++两种实现路径介绍

这里就明确两者是并列介绍关系。

------

## 第2页：为什么需要NPU仿真器

先从研发痛点出发：

- 编译器生成NPU专用指令流；
- 指令之间存在复杂的数据、存储和同步关系；
- RTL、FPGA和芯片资源成本较高；
- 硬件环境可观测性相对有限；
- 编译器需要快速验证生成结果；
- Runtime需要在真实硬件前验证接口和内存行为。

引出仿真器价值：

- 提前执行编译器生成的指令；
- 检查功能结果；
- 观察指令和数据流；
- 定位地址、同步和依赖问题；
- 降低软硬件联调成本；
- 为部分性能分析提供软件环境。

本页结论：

> 仿真器是在真实硬件之前，对NPU程序进行低成本、可观察、可重复验证的软件执行环境。

------

## 第3页：仿真器在研发工具链中的位置

建议图示：

```
模型 / 算子
     │
     ▼
NPU编译器
  图优化、算子融合、切分、调度、代码生成
     │
     │ 指令流、参数、权重、输入数据
     ▼
NPU仿真器
  指令执行、存储访问、同步、Trace、结果检查
     │
     ├── 功能结果
     ├── 指令Trace
     ├── 内存Dump
     └── 依赖/冲突报告
     │
     ▼
RTL / FPGA / 芯片
  硬件实现验证与最终性能确认
```

强调三个系统的职责：

| 系统     | 主要回答的问题                  |
| -------- | ------------------------------- |
| 编译器   | 如何生成NPU程序                 |
| 仿真器   | 这个NPU程序按模型执行会发生什么 |
| RTL/芯片 | 硬件实际如何实现和运行          |

------

## 第4页：从单一实现到双实现路径

本页用于解释历史，但不进行优劣评判。

建议表述：

### 第一阶段：Python实现

项目初期需要快速建立ISA功能模型，因此采用Python：

- 开发和修改效率高；
- 便于表达张量计算；
- 便于观察寄存器和内存状态；
- 便于快速支持编译器调试；
- 逐步形成完整功能和诊断能力。

### 第二阶段：扩展C++实现

随着使用场景扩展，增加了新的工程需求：

- 常驻Daemon；
- 共享内存；
- Runtime直接接入；
- 确定性逻辑时钟；
- 批量任务处理；
- Port Fabric；
- 可配置性能模型。

因此形成C++实现路径。

建议总结：

> C++实现不是对Python实现价值的否定，而是针对不同工程需求形成的另一种实现形态。

------

## 第5页：当前双实现总体定位

建议只做特点对比，不下“谁更好”的结论。

| 维度        | Python实现                       | C++实现                         |
| ----------- | -------------------------------- | ------------------------------- |
| 初始侧重点  | 快速建立功能模型                 | 系统化运行与集成                |
| 运行形态    | Case入口、进程内或Python Daemon  | Resident Daemon、One-shot客户端 |
| 调度方式    | 串行或多线程执行                 | 确定性Tick/Event调度            |
| 数据计算    | NumPy、ml_dtypes                 | C++、Eigen及自定义转换          |
| 诊断特点    | 灵活、调试信息丰富               | 结构化Trace和Effect分析         |
| Runtime接入 | 提供可选Daemon路径               | POSIX共享内存和异步Launch       |
| 性能分析    | 可配置Cost，部分受宿主机调度影响 | 独立逻辑Tick，可进一步校准      |
| 典型特点    | 易修改、易观察                   | 确定性、常驻运行、ABI明确       |

底部结论：

> 两种实现共同服务于指令功能验证，但采用了不同的执行模型和工程组织方式。

------

# 四、第二篇：架构实现

这一篇建议遵循“先共性，后差异”的顺序。

如果直接先讲Python、再讲C++，听众容易以为是两个完全不同的系统。先讲共同功能模型，可以帮助听众建立统一理解。

## 第6页：两种实现共同的功能模型

建议图示：

```
输入
指令流 + 初始化数据 + Runtime参数
               │
               ▼
系统初始化
Device / Die / Core / Memory
               │
               ▼
指令执行
Fetch → Decode → Dispatch → Execute
               │
               ▼
数据与同步
Register / L2 / DDR / Mail / CT Sync
               │
               ▼
输出
Trace / Dump / Compare / Analysis
```

两种实现的共同职责：

- 解释ZEUS-V3指令；
- 维护Core和寄存器状态；
- 模拟多个执行单元；
- 执行数据搬运与计算；
- 维护L2和DDR数据；
- 处理mail和多核同步；
- 生成Trace和结果；
- 对编译器输出进行验证。

本页核心结论：

> 两种实现的功能对象相同，差异主要体现在软件架构和调度方式。

------

## 第7页：统一的模块分层

建议抽象成六层：

```
┌──────────────────────────────────────┐
│ 接入层：Case、CLI、Daemon、Runtime   │
├──────────────────────────────────────┤
│ 系统层：Device、Die、Core生命周期    │
├──────────────────────────────────────┤
│ 控制层：取指、译码、CT、指令分派     │
├──────────────────────────────────────┤
│ 执行层：LD/LW/ST/DT/VP/PE/SI/SO     │
├──────────────────────────────────────┤
│ 存储互连层：Register、L2、DDR、Fabric│
├──────────────────────────────────────┤
│ 诊断层：Trace、Dump、Compare、Analysis│
└──────────────────────────────────────┘
```

说明：

- 两种实现都能映射到这六层；
- 实现类和内部数据结构不同；
- 这六层是后续理解两种架构的共同坐标系。

------

## 第8页：Python实现总体架构

建议图示：

```
main.py / main_debug.py / Python Daemon
                  │
                  ▼
             Case Runner
       加载Case、控制执行生命周期
                  │
                  ▼
            NPUSimulator
       多Die、多Core组织与管理
                  │
          ┌───────┴───────┐
          ▼               ▼
       NPUCore        InProcessFabric
          │
          ▼
  Instruction Dispatcher
serial / parallel / parallel-pc
          │
          ▼
 CT / LD / LW / ST / DT / VP / PE / SI / SO
          │
          ▼
 Register / L2 / GDG / Weight DDR
          │
          ▼
 Trace / Race / Conflict / Dump / Compare
```

讲解重点：

- `NPUSimulator`组织多Die、多Core；
- `NPUCore`维护单Core状态；
- Dispatcher负责指令推进和executor协作；
- 多种执行模式满足不同调试需求；
- NumPy负责主要张量计算；
- 诊断能力直接嵌入执行过程。

------

## 第9页：Python执行流程

建议用10个步骤：

1. 从case目录加载`init.json`；
2. 加载指令、输入、权重和初始化数据；
3. 创建Simulator、Core、寄存器和内存；
4. 设置入口PC；
5. 取指并解析指令字段；
6. CT处理控制指令或分派数据指令；
7. Executor等待mail和执行条件；
8. 完成数据读取、计算和写回；
9. 更新mail、寄存器、PC和Trace；
10. 执行结束后Dump并比较Golden。

随后说明三种调度方式：

- `serial`：强调简单和确定；
- `parallel`：多个executor并行；
- `parallel-pc`：CT和executor并行推进。

本页不要深入Python线程细节，只需解释执行行为。

------

## 第10页：C++实现总体架构

建议图示：

```
外部Runtime / One-shot Client
                 │
                 ▼
          Resident Daemon
   Device注册、Launch队列、状态管理
                 │
                 ▼
         Per-Device Worker
                 │
                 ▼
         SystemSimulator
 Core、Memory、Fabric、同步上下文
                 │
                 ▼
          Tick Scheduler
                 │
       ┌─────────┴─────────┐
       ▼                   ▼
   CT Dispatch        Executor Units
                           │
                           ▼
                Data Executor Pipeline
                           │
              ┌────────────┴────────────┐
              ▼                         ▼
        Effect规划               Payload执行
              │                         │
              └────────────┬────────────┘
                           ▼
                Memory Router/Fabric
                           │
                           ▼
             Trace / Conflict / Result
```

讲解重点：

- Daemon作为执行服务入口；
- Runtime或One-shot负责准备resident memory；
- SystemSimulator组织系统资源；
- Tick Scheduler使用逻辑时间推进；
- Effect规划与Payload执行分层；
- Memory Router统一处理本地和远端访问。

------

## 第11页：C++执行流程

建议按Launch生命周期讲：

1. Runtime或One-shot创建并初始化共享内存；
2. 向Daemon注册Device；
3. 提交异步Launch和Core入口地址；
4. Daemon将任务放入Device队列；
5. Device Worker创建SystemSimulator执行环境；
6. Scheduler推进Core逻辑时间；
7. CT取指、译码并尝试分派；
8. Executor根据Latency、mail和资源状态执行；
9. Data Pipeline规划Memory Effect并按模式执行Payload；
10. Memory Router完成本地或远端访问；
11. 收集Trace、Effect和冲突信息；
12. 返回Launch状态，One-shot执行Dump和Golden Compare。

------

## 第12页：两种调度模型对比

这是两种架构最核心的差异，建议单独讲。

### Python

```
serial：
CT → Executor → 完成 → 下一条

parallel/parallel-pc：
CT线程 + 多个Executor Worker
由mail和同步关系协调
```

### C++

```
逻辑Tick/Event：
推进Executor
    ↓
提交已完成任务
    ↓
尝试CT分派
    ↓
处理同步和Trace
    ↓
推进到下一逻辑时间
```

对比表：

| 方面     | Python                           | C++                              |
| -------- | -------------------------------- | -------------------------------- |
| 时间基础 | 执行模式相关，部分使用wall-clock | 独立逻辑tick                     |
| 并发表达 | Python线程和队列                 | 状态机和离散事件                 |
| 执行顺序 | 并行模式受线程调度影响           | 固定规则，结果可重复             |
| 调试特点 | 更接近直接观察并发执行           | 更适合重放逻辑时序               |
| 性能建模 | 配置cost并观察执行               | latency、issue gap和逻辑关键路径 |

边界表述：

> 两种调度模型表达的是同一NPU并发语义的不同软件实现，不应简单等同于真实硬件线程或完整RTL流水线。

------

## 第13页：Executor体系

两种实现可以共用一页介绍：

| Executor | 主要功能                             |
| -------- | ------------------------------------ |
| CT       | 控制流、跳转、循环、寄存器配置和同步 |
| LD       | 将数据加载到L2                       |
| LW       | 加载权重及相关数据                   |
| ST       | 将数据从L2写回目标存储空间           |
| DT       | 数据搬运、重排和格式处理             |
| VP       | 向量计算、归约、广播和LUT            |
| PE       | 矩阵乘、卷积和累加计算               |
| SI/SO    | 流输入、流输出和数据连接             |

需要解释：

- CT负责控制；
- 其他executor负责数据工作；
- 指令之间通过mail和同步建立顺序；
- 多个executor可以重叠工作；
- 功能结果由寄存器和存储副作用体现。

------

## 第14页：存储系统和Topology

建议图示：

```
                  指令地址
                     │
                     ▼
                地址解析/路由
       ┌─────────────┼─────────────┐
       ▼             ▼             ▼
  Core-local L2   Local DDR    Port Window
                  GDG/Weight        │
                                    ▼
                              Fabric Topology
                                    │
                                    ▼
                             Remote Device
                         L2 / GDG / Weight DDR
```

说明：

- 每个Core有本地L2；
- GDG属于Device/Die级存储；
- Weight DDR属于Core；
- 本地地址直接访问当前Device；
- Port Window选择远端端口；
- Topology决定端口连接到哪个Device；
- Python和C++在具体地址接入方式上有所不同；
- C++主要采用`zeus3-port-v1`地址ABI。

------

## 第15页：同步与依赖机制

介绍共同语义：

- send mail；
- receive mail；
- executor FIFO；
- CT_SEND/CT_WAIT；
- Core间token；
- SISO传输；
- barrier和drain；
- memory happens-before。

核心表达：

```
上游指令完成写入
        │
        ▼
     发送Mail
        │
        ▼
下游指令满足接收条件
        │
        ▼
     开始执行
```

说明同步既影响：

- 功能正确性；
- 执行顺序；
- 并行度；
- 性能关键路径；
- 冲突分析结果。

------

## 第16页：Trace与分析能力

可以按目的分为四类：

| 类别     | 输出                         | 解决的问题               |
| -------- | ---------------------------- | ------------------------ |
| 执行跟踪 | PC、指令、Executor Trace     | 执行了什么、为何停顿     |
| 状态检查 | Register、L2、DDR Dump       | 数据从哪里开始错误       |
| 结果判断 | Golden Compare               | 最终结果是否符合预期     |
| 依赖分析 | Race、Conflict、Effect Graph | 是否缺少可证明的执行顺序 |

Python特点：

- 调试插桩灵活；
- 在线Race和Timing Stress能力较丰富；
- 适合查看中间状态。

C++特点：

- 支持结构化Effect Log；
- 支持二进制Timing Trace；
- 支持summary/detail形式的冲突报告；
- 可在不执行完整Payload时进行分析。

这里继续保持平行陈述，不评价谁更好。

------

# 五、第三篇：功能能力与边界

## 第17页：当前仿真器整体能做什么

建议分成五类：

1. 指令功能执行；
2. 编译器输出验证；
3. 多Executor和多Core同步；
4. 存储及远端地址验证；
5. Trace、结果和依赖诊断。

强调这些是两种实现共同形成的总体能力，个别诊断开关可能只存在于某一种实现。

------

## 第18页：Python与C++能力差异

建议明确区分三种状态：

- 两边都有；
- 主要由Python提供；
- 主要由C++提供。

| 能力                 | Python            | C++                |
| -------------------- | ----------------- | ------------------ |
| 指令功能执行         | 支持              | 支持               |
| Case目录执行         | 支持              | 通过One-shot支持   |
| Dump和Golden Compare | 支持              | 支持               |
| 多Executor执行       | 支持              | 支持               |
| 多Core同步           | 支持              | 支持               |
| 在线Race Detector    | 主要支持          | 当前不等价         |
| Timing Stress        | 主要支持          | 可通过Cost模型分析 |
| Resident Runtime     | 支持实验/兼容路径 | 主要支持           |
| POSIX Shared Memory  | 有运行时接入      | 原生主路径         |
| Port Fabric ABI      | 有相关能力        | 当前主路径         |
| 确定性Tick Trace     | 不作为主要模型    | 支持               |
| Analysis-only        | 部分分析能力      | 明确支持           |
| Full-debug           | 支持组合诊断      | 明确支持           |

表格标题建议叫“实现特点”，避免叫“优劣对比”。

------

## 第19页：不能做什么

保持前述四类边界：

1. 不能替代RTL；
2. 不能默认完成性能签核；
3. 不能证明所有输入和路径；
4. 不能自动定位编译器根因。

------

## 第20页：性能仿真的能力边界

由于前面讨论过C++性能仿真，这一页建议单独存在。

### 能够分析

- 指令Latency；
- Issue Gap；
- Executor并行重叠；
- Mail和Barrier等待；
- 抽象关键路径；
- 工作量相关Cost；
- 调度方案的相对差异。

### 当前不能默认保证

- 芯片绝对周期；
- DDR真实带宽；
- L2 bank冲突；
- NoC/Fabric拥塞；
- RTL全部流水细节；
- 功耗、温度和频率变化。

核心结论：

> 性能仿真能力来自时序模型，而不是C++语言本身；结果可信度取决于模型完整度和硬件校准程度。

------

## 第21页：仿真结果如何解读

保留“可以说明/不能说明”矩阵，作为跨团队使用约定。

------

## 第22页：场景选择建议

这里不是给两个版本排优先级，而是提供客观选择依据。

| 场景关注点           | 可考虑的实现                   |
| -------------------- | ------------------------------ |
| 快速修改功能语义     | Python                         |
| 查看丰富中间状态     | Python                         |
| 单Case交互调试       | Python                         |
| 多线程竞态观察       | Python                         |
| Resident Runtime联调 | C++                            |
| 共享内存和Daemon任务 | C++                            |
| 确定性逻辑时序       | C++                            |
| 结构化Effect分析     | C++                            |
| 最终功能结论         | 建议结合Golden和必要的交叉验证 |
| 最终硬件结论         | RTL、FPGA或芯片验证            |

表格底部强调：

> 选择取决于验证目标，而不是简单按照语言决定。

------

# 六、建议采用的开场和结束语

## 开场语

> ZEUS-V3仿真器最初以Python实现，目标是快速建立ISA功能模型、支持编译器调试。随着使用场景扩展到常驻运行、Runtime集成、确定性时序和跨Device访问，又形成了C++实现。今天的重点不是评价哪种语言更好，而是介绍两种实现如何表达同一套NPU执行语义，各自采用了什么架构，以及仿真结果能够支持哪些判断。

## 结束语

> Python和C++是当前仿真器的两种实现路径。两者共同覆盖指令执行、存储访问、同步、结果检查和诊断分析，但采用了不同的软件架构。无论使用哪一种实现，都需要区分功能正确性、依赖正确性、性能估计和硬件实际行为，避免超出模型边界解释仿真结果。

------

# 七、建议确定的最终叙述逻辑

综合你的三个确认，我推荐最终采用以下逻辑：

```
1. 从研发需求解释为什么需要仿真器
2. 说明仿真器在编译器和硬件之间的位置
3. 介绍Python实现的形成背景和贡献
4. 说明新需求如何推动C++实现出现
5. 明确两种实现是平行路径，不是替代关系
6. 建立两种实现共同的功能模型
7. 平行介绍Python和C++总体架构
8. 对比两种调度、接入和诊断机制
9. 介绍共同的Executor、存储和同步语义
10. 汇总整体功能能力
11. 说明两种实现各自的特点和适用场景
12. 重点声明仿真器不能做什么
13. 解释性能仿真的能力和边界
14. 给出仿真结果的正确解读方式
```

这个顺序能够兼顾历史、两种架构和功能边界，同时避免把PPT讲成“Python与C++技术选型辩论”。